import { useEffect,useMemo,useState } from 'react'
import { apiRequest } from '../api/client'
import { AccountingNav } from '../components/AccountingNav'
const money=v=>`৳${Number(v).toLocaleString('en-BD',{minimumFractionDigits:2,maximumFractionDigits:2})}`
export function MoneyJournalPage(){const[entries,setEntries]=useState([]);const[error,setError]=useState('');useEffect(()=>{apiRequest('/accounting/journal').then(setEntries).catch(e=>setError(e.message))},[]);const totals=useMemo(()=>entries.reduce((a,e)=>({debit:a.debit+Number(e.debit),credit:a.credit+Number(e.credit)}),{debit:0,credit:0}),[entries]);return <div className="page-stack accounting-page"><AccountingNav/><div><p className="eyebrow">Automatic accounting</p><h1>Money journal</h1><p>Every sale and expense is translated into an accounting entry automatically. You do not need to enter debit or credit yourself.</p></div><section className="accounting-explainer"><article><strong>Debit</strong><p>Shows where the value went or what the business received. For a sale, the business receives cash.</p></article><article><strong>Credit</strong><p>Shows where the value came from. For a sale, it came from sales revenue.</p></article><article><strong>Simple rule</strong><p>Every entry must have equal debit and credit amounts. This keeps the records balanced.</p></article></section>{error&&<p className="error-message">{error}</p>}<section className="panel"><div className="panel-title-row"><div><p className="eyebrow">Generated automatically</p><h2>Transaction entries</h2></div><span className={totals.debit===totals.credit?'balanced-pill':'warning-pill'}>{totals.debit===totals.credit?'Balanced':'Needs attention'}</span></div><div className="table-scroll"><table><thead><tr><th>Time</th><th>What happened</th><th>Debit (value received/used)</th><th>Credit (value source)</th><th>Amount</th><th>Plain explanation</th></tr></thead><tbody>{entries.map((e,i)=><tr key={`${e.occurredAt}-${i}`}><td>{new Date(e.occurredAt).toLocaleString()}</td><td><strong>{e.description}</strong><small className="table-subtext">{e.source}</small></td><td>{e.debitAccount}</td><td>{e.creditAccount}</td><td>{money(e.debit)}</td><td>{e.plainExplanation}</td></tr>)}{!entries.length&&<tr><td colSpan="6">Record a sale or expense to generate the first journal entry.</td></tr>}</tbody></table></div></section></div>}
